# 🚀 Контент-Машина

**AI-генератор контент-планов для социальных сетей**

Платформа для автоматизированного создания контент-планов и постов для Telegram, Instagram, VK и Threads с помощью AI (OpenAI, Anthropic).

---

## 📋 Стек технологий

| Компонент       | Технология                                   |
| --------------- | -------------------------------------------- |
| **Frontend**    | Next.js 16 (App Router), React 19            |
| **Стили**       | Tailwind CSS v4, OKLCH тёмная тема           |
| **UI**          | shadcn/ui, Lucide Icons, Iconify             |
| **Backend**     | Next.js Server Actions, API Routes           |
| **База данных** | PostgreSQL 17 + Prisma ORM                   |
| **Авторизация** | NextAuth v5 (Auth.js), JWT, Credentials      |
| **AI**          | OpenAI SDK, Anthropic SDK                    |
| **Деплой**      | Docker, Docker Compose                       |

---

## 🚀 Быстрый старт (Docker)

### 1. Клонирование

```bash
git clone https://github.com/YOUR_USERNAME/content-machine.git
cd content-machine
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env — установите секреты и API ключи
```

### 3. Запуск

```bash
docker compose up -d --build
```

### 4. Инициализация базы

```bash
# Миграции
docker compose exec app npx prisma migrate deploy

# Или push схемы (для первого запуска)
docker compose exec app npx prisma db push

# Seed (создаёт демо-пользователя и рубрики)
docker compose exec app npx tsx prisma/seed.ts
```

### 5. Готово!

Откройте `http://YOUR_SERVER_IP:3000`

**Демо-аккаунт:**
- Email: `demo@contentmachine.ru`
- Пароль: `demo1234`

---

## 📁 Структура проекта

```
src/
├── app/
│   ├── (auth)/           # Логин, регистрация
│   ├── (dashboard)/      # Основные страницы
│   │   ├── dashboard/    # Дашборд
│   │   ├── calendar/     # Контент-календарь
│   │   ├── content-plan/ # Создание контент-плана
│   │   │   └── rubrics/  # Управление рубриками
│   │   └── settings/     # Настройки + AI
│   ├── api/auth/         # NextAuth API
│   ├── layout.tsx        # Корневой layout
│   └── globals.css       # Tailwind v4 дизайн-система
├── actions/              # Server Actions
├── components/
│   ├── calendar/         # Компоненты календаря
│   ├── content-plan/     # Компоненты контент-плана
│   └── layout/           # Sidebar
├── lib/
│   ├── ai/               # AI провайдеры и промпты
│   ├── auth.ts           # NextAuth конфигурация
│   ├── db.ts             # Prisma клиент
│   └── utils.ts          # Утилиты
└── types/                # TypeScript типы
```

---

## 🌐 Деплой на сервер

### С определённым портом

```bash
# В .env
APP_PORT=8080  # Ваш порт

docker compose up -d --build
```

### С Nginx reverse proxy

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 📊 Возможности

- ✅ **Создание контент-планов** — выбор соцсетей, рубрик, частоты, AI модели
- ✅ **Контент-календарь** — месячный вид, фильтрация по соцсетям
- ✅ **Редактор постов** — тексты, хештеги, статусы
- ✅ **Управление рубриками** — создание, редактирование, удаление
- ✅ **AI генерация** — OpenAI и Anthropic с выбором модели
- ✅ **Авторизация** — регистрация и вход
- ✅ **Тёмная тема** — OKLCH дизайн-система
- ✅ **Docker** — готовый к production деплой
